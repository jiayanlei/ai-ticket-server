spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ai_ticket
    username: lei
    password:

  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3s


  sql:
    init:
      mode: never

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    root: info
    com.aiticket.server: debug
